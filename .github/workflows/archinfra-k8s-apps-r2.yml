name: Archinfra Kubernetes Cluster Apps r2

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "archinfra/releases/k8s-v1.36.4-r2.env"
      - "archinfra/scripts/build-app.sh"
      - "archinfra/scripts/publish-app-manifest.sh"
      - "applications/helm/v4.2.4/**"
      - "applications/cilium/v1.20.1/**"
      - ".github/workflows/archinfra-k8s-apps-r2.yml"
  push:
    branches:
      - main
    paths:
      - "archinfra/releases/k8s-v1.36.4-r2.env"
      - "archinfra/scripts/build-app.sh"
      - "archinfra/scripts/publish-app-manifest.sh"
      - "applications/helm/v4.2.4/**"
      - "applications/cilium/v1.20.1/**"
      - ".github/workflows/archinfra-k8s-apps-r2.yml"

permissions:
  contents: read
  packages: write

concurrency:
  group: archinfra-k8s-cluster-apps-r2
  cancel-in-progress: false

env:
  RELEASE_FILE: ${{ github.workspace }}/archinfra/releases/k8s-v1.36.4-r2.env

jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate deterministic dual-arch application release
        shell: bash
        run: |
          set -euo pipefail
          bash -n archinfra/scripts/build-app.sh
          bash -n archinfra/scripts/publish-app-manifest.sh
          bash -n applications/helm/v4.2.4/init.sh
          bash -n applications/cilium/v1.20.1/init.sh
          bash -n applications/cilium/v1.20.1/entrypoint.sh

          source archinfra/releases/k8s-v1.36.4-r2.env
          test "$RELEASE_VERSION" = "k8s-v1.36.4-r2"
          test "$TARGET_ARCH" = "amd64"
          test "$KUBERNETES_VERSION" = "1.36.4"
          test "$SEALOS_VERSION" = "5.1.1"
          test "$HELM_VERSION" = "v4.2.4"
          test "$CILIUM_VERSION" = "v1.20.1"
          test "$CILIUM_CLI_VERSION" = "v0.19.7"
          test "$HUBBLE_CLI_VERSION" = "v1.19.4"
          test "$CILIUM_KUBE_PROXY_REPLACEMENT" = "false"
          test "$CILIUM_CHART_GIT_COMMIT" = "a0301d51baf1f88c9e27c322d1dac5fee2bc149b"

          for v in \
            KUBERNETES_RUNTIME_IMAGE KUBERNETES_RUNTIME_DIGEST \
            KUBERNETES_RUNTIME_IMAGE_ARM64 KUBERNETES_RUNTIME_DIGEST_ARM64 \
            HELM_URL_AMD64 HELM_SHA256_AMD64 HELM_URL_ARM64 HELM_SHA256_ARM64 \
            CILIUM_CLI_URL_AMD64 CILIUM_CLI_SHA256_AMD64 CILIUM_CLI_URL_ARM64 CILIUM_CLI_SHA256_ARM64 \
            HUBBLE_CLI_URL_AMD64 HUBBLE_CLI_SHA256_AMD64 HUBBLE_CLI_URL_ARM64 HUBBLE_CLI_SHA256_ARM64; do
            test -n "${!v}"
          done

          grep -F 'kubeProxyReplacement=false' applications/cilium/v1.20.1/entrypoint.sh
          if grep -R -E 'releases/latest|/master/' applications/helm/v4.2.4 applications/cilium/v1.20.1; then
            echo "floating source reference found in deterministic application sources" >&2
            exit 1
          fi
          echo "dual-arch application release lock validation OK"

  build:
    if: ${{ github.event_name != 'pull_request' }}
    needs: validate
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        app: [helm, cilium]
        arch: [amd64, arm64]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install deterministic build host tools
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            buildah \
            skopeo \
            jq \
            file \
            uidmap \
            fuse-overlayfs \
            ca-certificates \
            curl \
            python3-yaml

          # Host tools execute on the GitHub amd64 runner even for an arm64 target.
          source archinfra/releases/k8s-v1.36.4-r2.env

          curl -fsSL "$SEALOS_URL_AMD64" -o /tmp/sealos.tgz
          echo "$SEALOS_SHA256_AMD64  /tmp/sealos.tgz" | sha256sum -c -
          rm -rf /tmp/sealos-release
          mkdir -p /tmp/sealos-release
          tar -xzf /tmp/sealos.tgz -C /tmp/sealos-release
          SEALOS_BIN="$(find /tmp/sealos-release -type f -name sealos | head -n1)"
          test -n "$SEALOS_BIN"
          sudo install -m 0755 "$SEALOS_BIN" /usr/local/bin/sealos

          curl -fsSL "$HELM_URL_AMD64" -o /tmp/helm.tgz
          echo "$HELM_SHA256_AMD64  /tmp/helm.tgz" | sha256sum -c -
          rm -rf /tmp/helm-release
          mkdir -p /tmp/helm-release
          tar -xzf /tmp/helm.tgz -C /tmp/helm-release linux-amd64/helm
          sudo install -m 0755 /tmp/helm-release/linux-amd64/helm /usr/local/bin/helm

          sealos version
          helm version --short
          buildah --version
          skopeo --version

      - name: Build, verify, publish and mirror ${{ matrix.app }} ${{ matrix.arch }} Cluster Image
        shell: bash
        env:
          GHCR_USER: ${{ github.actor }}
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PUBLISH_ALIYUN: ${{ vars.PUBLISH_ALIYUN }}
          ALIYUN_REGISTRY: ${{ vars.ALIYUN_REGISTRY }}
          ALIYUN_NAMESPACE: ${{ vars.ALIYUN_NAMESPACE }}
          ALIYUN_USERNAME: ${{ secrets.ALIYUN_USERNAME }}
          ALIYUN_PASSWORD: ${{ secrets.ALIYUN_PASSWORD }}
        run: bash archinfra/scripts/build-app.sh "${{ matrix.app }}" "${{ matrix.arch }}"

      - name: Upload ${{ matrix.app }} ${{ matrix.arch }} provenance
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kubernetes-cluster-image-${{ matrix.app }}-r2-${{ matrix.arch }}-provenance
          path: out/
          if-no-files-found: warn

  manifest:
    if: ${{ github.event_name != 'pull_request' }}
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        app: [helm, cilium]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Install manifest verification tools
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends skopeo jq
          docker buildx version
          skopeo --version

      - name: Publish and verify ${{ matrix.app }} multi-arch manifest
        shell: bash
        env:
          GHCR_USER: ${{ github.actor }}
          GHCR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PUBLISH_ALIYUN: ${{ vars.PUBLISH_ALIYUN }}
          ALIYUN_REGISTRY: ${{ vars.ALIYUN_REGISTRY }}
          ALIYUN_NAMESPACE: ${{ vars.ALIYUN_NAMESPACE }}
          ALIYUN_USERNAME: ${{ secrets.ALIYUN_USERNAME }}
          ALIYUN_PASSWORD: ${{ secrets.ALIYUN_PASSWORD }}
        run: bash archinfra/scripts/publish-app-manifest.sh "${{ matrix.app }}"

      - name: Upload ${{ matrix.app }} multi-arch provenance
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: kubernetes-cluster-image-${{ matrix.app }}-r2-multiarch-provenance
          path: out/
          if-no-files-found: warn
